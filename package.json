{
  "name": "iamges.hasanjoldic.com",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci -- --legacy-peer-deps",
    "init": "./scripts/init.sh",
    "services-start-watch": "./scripts/services-start-watch.sh"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "services/*"
    ]
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "typescript": "^4.8.3"
  }
}